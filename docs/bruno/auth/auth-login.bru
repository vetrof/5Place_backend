meta {
  name: auth/login
  type: http
  seq: 2
}

post {
  url: {{domain}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@example.com",
    "password": "password123"
  }
}
