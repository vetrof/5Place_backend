-- +goose Up
CREATE TABLE IF NOT EXISTS app_photo (
	id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
	place_id INTEGER REFERENCES app_place(id) ON UPDATE CASCADE ON DELETE CASCADE,
	image TEXT,
	description TEXT
);

-- +goose Down
DROP TABLE IF EXISTS app_photo;
