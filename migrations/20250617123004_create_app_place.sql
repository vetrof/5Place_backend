-- +goose Up
CREATE TABLE IF NOT EXISTS app_place (
	id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
	city_id INTEGER REFERENCES app_city(id) ON UPDATE CASCADE ON DELETE SET NULL,
	name TEXT,
	geom GEOGRAPHY(POINT,4326),
	descr TEXT,
	author INTEGER REFERENCES app_user(id) ON UPDATE CASCADE ON DELETE SET NULL,
	type_id INTEGER REFERENCES app_place_type(id) ON UPDATE CASCADE ON DELETE SET NULL
);

-- +goose Down
DROP TABLE IF EXISTS app_place;
